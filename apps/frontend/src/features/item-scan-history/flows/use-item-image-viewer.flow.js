export function useItemImageViewerFlow() {
    // Kept as a compatibility no-op. The image viewer is now rendered as a
    // global overlay in HomeFeature so it does not replace active full pages.
}
